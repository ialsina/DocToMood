# Requirements for building standalone executables
pyinstaller>=6.0.0

# PyInstaller runtime dependencies
platformdirs
setuptools

# Application dependencies (also needed for building)
python-docx
odfpy
pandas
pyyaml
tqdm
